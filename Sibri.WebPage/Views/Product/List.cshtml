@model Sibri.Contract.pages.Pages
@{
    ViewData["Title"] = Model.Title;
}
@section Header{
    @await Component.InvokeAsync("Top")
}
@section Footer{
    @await Component.InvokeAsync("Bottom")
}
@await Component.InvokeAsync("Banner", new { categoryid = Model.ColumnID })
@await Component.InvokeAsync("Product", new { categoryid = 6, direction = 1 })
<div id="q_neipro_bu50" class="nei50"></div>
<div id="q_neipro_10">
    <div id="mid_nei10_01" class="top50">
        @await Component.InvokeAsync("Left", new { view = "Product", categoryid = Model.ColumnID, direction = 1 })
        <div id="nei10_01_right">
            <div class="protit01 margintop20">@Model.Title</div>
            <div>
                @Html.Raw(@Model.PContent)
            </div>
        </div>
    </div>
</div>
<div class="height30kong"></div>
@section Scripts{
    <script>
        $(function () {
            $('#tit>div').eq(1).addClass('select').siblings().removeClass('select');
            $('#con div').eq(1).show().siblings().hide();
            $('#neidaohangline').show();
        })
    </script>
}

